apiVersion: v1
kind: Service
metadata:
  name: graphql-service
  namespace: p-microservices
spec:
  type: LoadBalancer
  selector:
    app: graphql
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3410
