apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: p-microservices
spec:
  type: LoadBalancer
  selector:
    app: order
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3405
